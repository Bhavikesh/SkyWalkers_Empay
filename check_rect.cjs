const puppeteer = require('puppeteer'); (async () => { const browser = await puppeteer.launch(); const page = await browser.newPage(); await page.goto('http://localhost:5173/attendance', {waitUntil: 'networkidle0'}); const rect = await page.evaluate(() => { const main = document.querySelector('main'); return main ? JSON.stringify(main.getBoundingClientRect()) : 'no main'; }); console.log('MAIN RECT:', rect); await browser.close(); })();
